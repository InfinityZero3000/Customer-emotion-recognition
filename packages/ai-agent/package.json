{
  "name": "@repo/ai-agent",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "langchain": "^0.2.0",
    "@langchain/core": "^0.2.0",
    "@langchain/openai": "^0.2.0",
    "@repo/shared-types": "workspace:*",
    "@repo/vector-db": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
